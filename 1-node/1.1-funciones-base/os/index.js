const os = require('os');

console.log(os.version()); // muestra versión del kernel
console.log(os.arch()); // obtiene arquitecturas
console.log(os.cpus()); //
console.log(os.freemem());
console.log(os.homedir());
